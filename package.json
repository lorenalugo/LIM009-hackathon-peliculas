{
  "name": "movie-hackathon",
  "version": "1.0.0",
  "description": "final common core project, Laboratoria",
  "main": "src/index.html",
  "license": "MIT",
  "scripts": {
    "htmlhint": "htmlhint src/*.html test/*.html",
    "eslint": "eslint --ext .js src/ test/",
    "pretest": "npm run eslint && npm run htmlhint",
    "test": "jest --coverage"
  },
  "dependencies": {
    "eslint": "^5.15.3",
    "eslint-plugin-jest": "^22.1.2",
    "htmlhint": "^0.10.1"
  },
  "devDependencies": {
    "jest": "^24.5.0"
  }
}
